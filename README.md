# pdf-microservice

Installation Guide:
1. Install Docker Destkop
2. Pull dotnet image: docker pull mcr.microsoft.com/dotnet/sdk:9.0
3. Clone this repo
4. Npm install package & run dev in frontend
5. Build & Start the dotnet app image: docker-compose up --build -d
